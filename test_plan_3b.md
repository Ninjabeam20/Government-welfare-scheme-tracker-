<div class="page-title">

# Government Welfare Eligibility & Tracking System
### Laboratory-10: Comprehensive Test Plan Document — Part III-B

<br>

**Sections Covered:** 9. Test Cases (Subsystems 3 & 4) · 10. Testing Schedule

</div>

<div style="page-break-after: always;"></div>

---

## 9.3 Subsystem 3: Scheme Discovery & Application Processing

### 9.3.1 Unit Tests (White-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| SCHM-UT-01 | Verify `GET /api/citizen/schemes` returns only active schemes | Backend + DB running. Seed data contains: `National Health Grant` (`isactive=1`) and `Education Scholarship` (`isactive=0`). | 1. Open Postman. 2. Send `GET http://localhost:5000/api/citizen/schemes`. 3. Inspect response array. | Response: `200 OK`. Array contains exactly **one** scheme object: `National Health Grant`. `Education Scholarship` is **absent**. Each object has fields: `SchemeID`, `SchemeName`, `Description`, `isactive`, `max_income`. | |
| | | | | **[INSERT SCREENSHOT: Postman response showing only the active scheme (National Health Grant) returned from /api/citizen/schemes]** | |
| SCHM-UT-02 | Verify inactive scheme is excluded from citizen scheme list | Backend + DB running. `Education Scholarship` has `isactive = 0`. | 1. Run SQL: `SELECT * FROM welfareschemes WHERE isactive = 0;` — confirm `Education Scholarship` is inactive. 2. Send `GET /api/citizen/schemes`. 3. Verify `Education Scholarship` is absent in the response. | SQL confirms inactive status. API response does not include `Education Scholarship`. The SQL query `WHERE isactive = 1` correctly filters the result set. | |
| | | | | **[INSERT SCREENSHOT: MySQL confirming Education Scholarship isactive=0, alongside Postman response excluding it]** | |
| SCHM-UT-03 | Verify `GET /api/citizen/track/:recordId` returns application details | Backend + DB running. An application with `RecordID = 1` exists (inserted via seed or prior test). | 1. Send `GET http://localhost:5000/api/citizen/track/1`. 2. Inspect response. | Response: `200 OK`. Body: `{"RecordID": 1, "SchemeName": "National Health Grant", "Status": "Pending", "Applied_on": "<timestamp>"}`. | |
| | | | | **[INSERT SCREENSHOT: Postman showing track response with RecordID, SchemeName, Status, and Applied_on]** | |
| SCHM-UT-04 | Verify track endpoint returns 404 for non-existent RecordID | Backend running. No application with `RecordID = 9999`. | 1. Send `GET http://localhost:5000/api/citizen/track/9999`. 2. Inspect response. | Response: `404 Not Found`. Body: `{"message": "Application not found"}`. | |
| | | | | **[INSERT SCREENSHOT: Postman showing 404 response for non-existent RecordID 9999]** | |
| SCHM-UT-05 | Verify `Applied_on` auto-populated via `CURRENT_TIMESTAMP` | Backend + DB running. Citizen `BeneficiaryID=1` and active `SchemeID=1` exist. | 1. Send `POST http://localhost:5000/api/citizen/apply` with body `{"citizen_id": 1, "scheme_id": 1}`. 2. Run SQL: `SELECT Applied_on FROM currentapplications ORDER BY RecordID DESC LIMIT 1;` | The `Applied_on` column contains a valid timestamp closely matching the current server time. The value was not supplied by the client — it was auto-generated by MySQL `DEFAULT CURRENT_TIMESTAMP`. | |
| | | | | **[INSERT SCREENSHOT: MySQL Workbench showing Applied_on timestamp auto-generated in the new currentapplications row]** | |

### 9.3.2 Integration Tests (White-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| SCHM-IT-01 | Verify `POST /api/citizen/apply` inserts a row with Status='Pending' | Backend + DB running. `BeneficiaryID=1`, `SchemeID=1` exist. Re-seed DB to clear prior applications. | 1. Note current row count: `SELECT COUNT(*) FROM currentapplications;` 2. Send `POST http://localhost:5000/api/citizen/apply` with body `{"citizen_id": 1, "scheme_id": 1}`. 3. Inspect API response. 4. Run SQL: `SELECT * FROM currentapplications ORDER BY RecordID DESC LIMIT 1;` | API response: `200 OK`, `{"success": true, "message": "Application submitted!"}`. New row in `currentapplications` with `BeneficiaryID=1`, `SchemeID=1`, `Status='Pending'`. Row count incremented by 1. | |
| | | | | **[INSERT SCREENSHOT: Postman showing 200 OK from /apply, AND MySQL showing new Pending row in currentapplications]** | |
| SCHM-IT-02 | Verify FOREIGN KEY constraint rejects invalid SchemeID | Backend + DB running. No scheme with `SchemeID=9999`. | 1. Send `POST http://localhost:5000/api/citizen/apply` with body `{"citizen_id": 1, "scheme_id": 9999}`. 2. Inspect response. | Response: `500 Internal Server Error`. Body: `{"message": "Error submitting application"}`. The MySQL InnoDB engine rejected the insert due to `FOREIGN KEY` constraint violation on `SchemeID`. No row created in `currentapplications`. | |
| | | | | **[INSERT SCREENSHOT: Postman showing 500 error response when submitting with non-existent scheme_id=9999]** | |
| SCHM-IT-03 | Verify `GET /api/citizen/applications/:beneficiaryId` returns correct joined data | `BeneficiaryID=1` has at least one application submitted (from SCHM-IT-01). | 1. Send `GET http://localhost:5000/api/citizen/applications/1`. 2. Inspect the response array. | Response: `200 OK`. Array contains at least one object with fields: `RecordID`, `SchemeName` (joined from `welfareschemes`), `Status`, `Applied_on`. Results are ordered by `Applied_on DESC`. | |
| | | | | **[INSERT SCREENSHOT: Postman response from /api/citizen/applications/1 showing joined SchemeName and Status]** | |
| SCHM-IT-04 | Verify eligibility boundary — citizen income at scheme max_income limit | `National Health Grant` has `max_income=50000`. Citizen `John Doe` has `Income=45000` (below limit). | 1. Send `POST /api/citizen/apply` with `{"citizen_id": 1, "scheme_id": 1}`. 2. Check API response. 3. Verify row in `currentapplications`. | Application accepted. `Status = 'Pending'`. This confirms the system allows applications for citizens whose income is below or equal to `max_income`. *(Note: Income validation is currently UI-guided; backend accepts all submissions — document this as an observation.)* | |
| | | | | **[INSERT SCREENSHOT: Postman showing successful apply response for income-eligible citizen]** | |

### 9.3.3 System Tests (Black-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| SCHM-ST-01 | Citizen browses active schemes on UI | Citizen logged in. Frontend + Backend running. | 1. Login as a citizen. 2. Navigate to "Browse Schemes" or equivalent section. 3. Observe the list of schemes displayed. | Only active schemes are displayed (e.g., `National Health Grant`). Inactive schemes (e.g., `Education Scholarship`) do not appear. Each scheme card shows its name, description, and eligibility info. | |
| | | | | **[INSERT SCREENSHOT: Citizen portal UI showing the Browse Schemes page with only active schemes listed]** | |
| SCHM-ST-02 | Citizen applies for a scheme and receives confirmation | Citizen logged in. At least one active scheme visible. | 1. Click "Apply" on the `National Health Grant` scheme card. 2. Confirm or submit the application form. 3. Observe the UI response after submission. | A success confirmation message is displayed (e.g., "Application submitted!"). No page crash or error message. The citizen is able to see the new application in their applications list. | |
| | | | | **[INSERT SCREENSHOT: Citizen portal showing success message after clicking Apply]** | |
| SCHM-ST-03 | Citizen tracks application status by RecordID | Citizen has a submitted application. The Track Application feature is accessible. | 1. Navigate to "Track Application" section. 2. Enter the `RecordID` of a submitted application. 3. Click "Track". 4. Observe the result displayed. | Application details displayed: `SchemeName`, `Status = 'Pending'`, `Applied_on` timestamp. UI correctly reflects the database state. | |
| | | | | **[INSERT SCREENSHOT: Citizen portal Track Application screen showing Pending status with scheme name and date]** | |
| SCHM-ST-04 | Citizen application history shows all submissions | Citizen has multiple applications submitted. | 1. Login as citizen. 2. Navigate to application history / "My Applications" section. 3. Observe the list. | All submitted applications are listed with their `SchemeName` and current `Status`. List is ordered with the most recent application at the top. | |
| | | | | **[INSERT SCREENSHOT: Citizen portal My Applications page showing a list of applications with statuses]** | |

<div style="page-break-after: always;"></div>

---

## 9.4 Subsystem 4: Workflow Automation & Decision Management

### 9.4.1 Unit Tests (White-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| WKFL-UT-01 | Verify `PUT /api/officer/applications/:id/approve` updates Status to 'Approved' | Backend + DB running. Application with `RecordID=1` exists with `Status='Pending'`. | 1. Open Postman. 2. Send `PUT http://localhost:5000/api/officer/applications/1/approve`. 3. Inspect API response. 4. Run SQL: `SELECT Status FROM currentapplications WHERE RecordID=1;` | API response: `200 OK`, `{"success": true, "message": "Application 1 successfully marked as Approved."}`. SQL confirms `Status = 'Approved'` in the database. | |
| | | | | **[INSERT SCREENSHOT: Postman showing 200 approve response AND MySQL confirming Status='Approved']** | |
| WKFL-UT-02 | Verify `PUT /api/officer/applications/:id/reject` updates Status to 'Rejected' | Backend + DB running. Re-seed DB. Application with `RecordID=1` is `Status='Pending'`. | 1. Send `PUT http://localhost:5000/api/officer/applications/1/reject`. 2. Inspect response. 3. Run SQL: `SELECT Status FROM currentapplications WHERE RecordID=1;` | API response: `200 OK`, `{"success": true, "message": "Application 1 successfully marked as Rejected."}`. SQL confirms `Status = 'Rejected'`. | |
| | | | | **[INSERT SCREENSHOT: Postman showing 200 reject response AND MySQL confirming Status='Rejected']** | |
| WKFL-UT-03 | Verify audit log entry created on Approve action | Backend + DB running. Note current `auditlogs` row count before test. `RecordID=1` is `Status='Pending'`. | 1. Send `PUT /api/officer/applications/1/approve`. 2. Run SQL: `SELECT * FROM auditlogs ORDER BY LogID DESC LIMIT 1;` | A new row is inserted in `auditlogs` with: `ActorType = 'Officer'`, `ActionDetails = 'Approved application #1'`. `ActionTime` is close to current server time. Row count incremented by 1. | |
| | | | | **[INSERT SCREENSHOT: MySQL Workbench showing new auditlogs row with ActorType=Officer and Approved application #1]** | |
| WKFL-UT-04 | Verify audit log entry created on Reject action | Backend + DB running. `RecordID=1` is `Status='Pending'` (re-seed). | 1. Send `PUT /api/officer/applications/1/reject`. 2. Run SQL: `SELECT * FROM auditlogs ORDER BY LogID DESC LIMIT 1;` | New `auditlogs` row: `ActorType = 'Officer'`, `ActionDetails = 'Rejected application #1'`. | |
| | | | | **[INSERT SCREENSHOT: MySQL Workbench showing auditlogs row for Rejected application #1]** | |

### 9.4.2 Integration Tests (White-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| WKFL-IT-01 | Verify officer queue returns all Pending applications with joined data | Backend + DB running. At least one application with `Status='Pending'` exists. | 1. Send `GET http://localhost:5000/api/officer/queue/1`. 2. Inspect response array. | Response: `200 OK`. Array contains application objects each with: `RecordID`, `Name` (citizen), `Email`, `Aadhaar`, `Income`, `SchemeName`, `Applied_on`, `Status='Pending'`. Data is correctly joined from `individualbeneficiaries` and `welfareschemes`. | |
| | | | | **[INSERT SCREENSHOT: Postman response from /api/officer/queue/1 showing pending applications with joined citizen and scheme data]** | |
| WKFL-IT-02 | Verify approved application disappears from the Pending queue | Backend + DB running. Application `RecordID=1` is `Status='Pending'`. | 1. `GET /api/officer/queue/1` — note `RecordID=1` is present. 2. `PUT /api/officer/applications/1/approve`. 3. `GET /api/officer/queue/1` again. 4. Inspect response. | After approval, `RecordID=1` is **absent** from the queue response. The queue query filters `WHERE Status = 'Pending'` — since status is now `'Approved'`, the application is excluded. | |
| | | | | **[INSERT SCREENSHOT: Two Postman responses for /officer/queue — first showing RecordID=1, second showing it absent after approval]** | |
| WKFL-IT-03 | Verify rejected application disappears from the Pending queue | Backend + DB running. Re-seed DB. `RecordID=1` is `Status='Pending'`. | 1. `GET /api/officer/queue/1` — note `RecordID=1` present. 2. `PUT /api/officer/applications/1/reject`. 3. `GET /api/officer/queue/1`. | After rejection, `RecordID=1` is **absent** from the queue. Same filter logic applies as in WKFL-IT-02. | |
| | | | | **[INSERT SCREENSHOT: Postman officer queue before and after rejection showing application removed]** | |
| WKFL-IT-04 | Verify end-to-end state propagation: Citizen sees approved status | Backend + DB running. `RecordID=1` approved via WKFL-UT-01. | 1. `PUT /api/officer/applications/1/approve` (if not already done). 2. `GET /api/citizen/applications/1` (citizen's application list for BeneficiaryID=1). 3. Inspect the `Status` field of the application in the response. | Response includes the application with `Status = 'Approved'`. The same `currentapplications` table is queried from both officer and citizen endpoints, guaranteeing a single source of truth. | |
| | | | | **[INSERT SCREENSHOT: Postman /api/citizen/applications/1 response showing Status='Approved' after officer action]** | |
| WKFL-IT-05 | Verify admin audit log records both officer decisions | Backend + DB running. At least one approve and one reject action performed (from earlier tests). | 1. Send `GET http://localhost:5000/api/admin/auditlogs`. 2. Inspect the response array. | Response: `200 OK`. Array contains entries with `ActorType='Officer'` for both approve and reject actions, with correct `ActionDetails` and `ActionTime`. Results ordered by `ActionTime DESC`. | |
| | | | | **[INSERT SCREENSHOT: Postman /api/admin/auditlogs response showing Officer approval and rejection log entries]** | |

### 9.4.3 System Tests (Black-Box)

| Test ID | Description | Pre-conditions | Steps | Expected Result | Pass/Fail |
|---------|-------------|----------------|-------|-----------------|-----------|
| WKFL-ST-01 | Officer views pending application queue on dashboard | Officer (`aarti@govt.in`) logged in via OAuth. At least one `Status='Pending'` application exists. | 1. Login as Officer. 2. Navigate to Officer Dashboard. 3. Observe the applications queue. | Officer Dashboard displays a list of pending applications. Each row shows citizen name, scheme name, income, and applied date. UI renders without errors. | |
| | | | | **[INSERT SCREENSHOT: Officer Dashboard UI showing the pending applications queue with citizen details]** | |
| WKFL-ST-02 | Officer approves an application — status updates in real time | Officer logged in. At least one `Status='Pending'` application visible in queue. | 1. Click "Approve" on an application in the officer queue. 2. Observe the queue after action. 3. Login as the corresponding Citizen. 4. Navigate to "My Applications". 5. Find the same application. | After approval: application disappears from the officer queue. In the Citizen portal, the same application shows `Status = 'Approved'` (or "Approved" badge). | |
| | | | | **[INSERT SCREENSHOT: Officer queue after approval (application gone) AND Citizen portal showing Approved status]** | |
| WKFL-ST-03 | Officer rejects an application — status updates in Citizen portal | Officer logged in. A different `Status='Pending'` application visible. | 1. Click "Reject" on an application. 2. Observe queue. 3. Login as the citizen whose application was rejected. 4. Check "My Applications". | Rejected application disappears from officer queue. Citizen portal shows `Status = 'Rejected'` for that application. | |
| | | | | **[INSERT SCREENSHOT: Citizen portal showing Rejected status after officer rejection action]** | |
| WKFL-ST-04 | Admin views complete audit log including officer decisions | Admin (`admin@govt.in`) logged in. At least one approve/reject action recorded. | 1. Login as Admin. 2. Navigate to Audit Logs section of Admin Dashboard. 3. Observe the log entries. | Audit log table displays entries with `ActorType`, `ActionDetails`, and `ActionTime`. Officer approval and rejection entries are visible with correct application IDs and timestamps. | |
| | | | | **[INSERT SCREENSHOT: Admin Dashboard Audit Log section showing Officer action entries with timestamps]** | |

<div style="page-break-after: always;"></div>

---

# 10. Testing Schedule

## 10.1 Overview

The testing activities are organized into four chronological phases mapped to the four targeted subsystems. This schedule assumes a single tester executing manual tests on a configured local development environment. Each phase includes time for setup, execution, evidence capture (screenshots), and defect logging.

## 10.2 Detailed Schedule

| Phase | Subsystem | Activities | Estimated Duration | Deliverables |
|-------|-----------|------------|-------------------|-------------|
| **Phase 0** | All Subsystems | **Environment Setup & Verification** — Install dependencies (`npm install`), seed database (`seed-database.sql`), start frontend and backend servers, verify health check endpoint (`/api/health`), clear all browser cookies and sessions. | 30 minutes | Verified running environment; health check screenshot. |
| **Phase 1** | Authentication & Access Control | **Unit Tests** (AUTH-UT-01 to AUTH-UT-04) — Postman API calls, session inspection. **Integration Tests** (AUTH-IT-01 to AUTH-IT-05) — OAuth login flow for each role, session persistence, RBAC boundary tests. **System Tests** (AUTH-ST-01 to AUTH-ST-03) — Browser-based login/logout, auto-registration. | 1.5 hours | Completed test case table with PASS/FAIL verdicts and screenshots for all 12 Auth test cases. |
| **Phase 2** | Citizen Enrollment & Profile Management | **Unit Tests** (ENRL-UT-01 to ENRL-UT-04) — Profile endpoint and DB constraint tests. **Integration Tests** (ENRL-IT-01 to ENRL-IT-03) — Auto-registration and session mapping. **System Tests** (ENRL-ST-01 to ENRL-ST-02) — Dashboard profile rendering. | 1 hour | Completed test case table with verdicts and screenshots for all 9 Enrollment test cases. |
| **Phase 3** | Scheme Discovery & Application Processing | **Unit Tests** (SCHM-UT-01 to SCHM-UT-05) — Scheme API filtering, track endpoint, timestamp verification. **Integration Tests** (SCHM-IT-01 to SCHM-IT-04) — Application submission, FK constraint, joined data. **System Tests** (SCHM-ST-01 to SCHM-ST-04) — Browse, apply, track, and history UI flows. | 1.5 hours | Completed test case table with verdicts and screenshots for all 13 Scheme test cases. |
| **Phase 4** | Workflow Automation & Decision Management | **Unit Tests** (WKFL-UT-01 to WKFL-UT-04) — Approve/reject API, audit log insertion. **Integration Tests** (WKFL-IT-01 to WKFL-IT-05) — Queue updates, state propagation, admin audit view. **System Tests** (WKFL-ST-01 to WKFL-ST-04) — Full end-to-end officer and citizen UI workflow. | 2 hours | Completed test case table with verdicts and screenshots for all 13 Workflow test cases. |
| **Phase 5** | All Subsystems | **Defect Logging & Test Summary** — Compile all PASS/FAIL results, log any discovered defects with severity, calculate pass rates per subsystem, and insert all captured screenshots into placeholders. | 1 hour | Final test summary table; defect log (if any); complete Test Plan document with evidence. |

## 10.3 Testing Timeline (7-Day View)

| Day | Activity |
|-----|----------|
| **Day 1** | Phase 0: Environment Setup + Phase 1: Authentication Testing |
| **Day 2** | Phase 2: Citizen Enrollment Testing |
| **Day 3** | Phase 3: Scheme Discovery Testing |
| **Day 4** | Phase 4: Workflow Automation Testing |
| **Day 5** | Phase 5: Defect Logging, Evidence Compilation |
| **Day 6** | Screenshot insertion, document formatting, review |
| **Day 7** | Final submission preparation and PDF export |

## 10.4 Test Case Summary

| Subsystem | Unit Tests | Integration Tests | System Tests | Total |
|-----------|-----------|-------------------|--------------|-------|
| Authentication & Access Control | 4 | 5 | 3 | **12** |
| Citizen Enrollment & Profile Management | 4 | 3 | 2 | **9** |
| Scheme Discovery & Application Processing | 5 | 4 | 4 | **13** |
| Workflow Automation & Decision Management | 4 | 5 | 4 | **13** |
| **TOTAL** | **17** | **17** | **13** | **47** |

## 10.5 Testing Responsibilities

| Role | Responsibility |
|------|---------------|
| **Tester (Student)** | Execute all 47 test cases, capture screenshot evidence, record PASS/FAIL verdicts, log defects. |
| **Developer (Student)** | Ensure test environment is correctly configured, resolve Severity-1/2 defects identified during testing. |
| **Lab Evaluator (Instructor)** | Review the completed Test Plan document with evidence against the grading rubric for Laboratory-10. |

---

<br>

<div style="text-align: center; color: #64748b; margin-top: 50px;">

---

**Complete Test Plan — Laboratory 10**

*Government Welfare Eligibility & Tracking System*

`test_plan_1.md` (Sections 1–3) · `test_plan_2.md` (Sections 4–6) · `test_plan_3a.md` (Sections 7–9, Subsystems 1 & 2) · `test_plan_3b.md` (Section 9, Subsystems 3 & 4 · Section 10)

*— End of Document —*

</div>
